# Android 日志采集工具

该工具适用于 Windows 电脑，用于采集 Android App 的普通运行日志，以及功能异常、闪退、白屏、卡死和 ANR 等问题的现场信息。它不依赖 Android 工程，也不要求安装 Android Studio。

## 使用前准备

1. 手机进入“开发者选项”，开启“USB 调试”。
2. 用可传输数据的 USB 线连接电脑。
3. 手机上弹出“是否允许 USB 调试”时，选择“允许”。
4. 打开待测试的 App，并停留在正常页面。

部分品牌手机首次连接 Windows 时需要安装该品牌的 USB 驱动。驱动属于设备厂商组件，无法由通用脚本自动安装。

## 操作步骤

1. 双击 `开始采集Android日志.bat`。
2. 工具按以下顺序查找 ADB：
   - 优先读取 `adb.config` 中配置的 `ADB_PATH`；
   - 配置留空时，从 Windows 的 `PATH` 环境变量查找 `adb.exe`；
   - 再检查工具之前下载到 `_runtime` 的 ADB；
   - 仍未找到时，询问是否从 Google 官方地址下载 `platform-tools`，不会未经确认自动下载。
3. 如果连接多台设备，按提示输入设备序号。
4. 确认目标 App 已在前台，按回车。工具会自动识别前台应用包名；识别正确时直接按回车，否则输入正确包名。
5. 根据提示选择是否同时录制设备屏幕。直接按回车或输入 `N` 表示不录屏；输入 `Y` 表示启用录屏。
6. 工具清空旧 Logcat 并开始持续抓取后，操作手机复现问题。选择录屏时，录屏会与 Logcat 同时启动。
7. 完成普通操作或复现问题后，不要退出 App，也不要锁屏、拔线或杀进程。
8. 回到工具窗口按回车。工具会先停止并导出录屏，再采集现场信息和完整 bugreport；bugreport 通常需要几分钟。
9. 将工具提示的 `AndroidLogs_日期_设备序列号.zip` 发给开发，同时提供复现步骤和大致发生时间。

录屏使用 Android 系统自带的 `screenrecord`：通常最长录制 180 秒，并且不包含设备音频。达到时长上限后录屏会自动结束，但 Logcat 会继续采集。录屏启动、停止或导出失败不会中断其他日志采集，具体结果会写入 `采集摘要.txt`。

执行下载、解压、现场信息采集、线程快照、bugreport 和日志压缩等耗时操作时，窗口底部会使用同一行显示动态进度，不会在窗口顶部反复刷新。短于 1 秒的操作不显示进度，避免快速闪烁；任务完成后会清除该行，再继续显示后续内容。部分 ADB 系统命令无法提供真实总量，此时进度用于表示工具仍在正常运行，并同时显示已耗时。

## 输出位置

每次采集都会生成独立目录和 ZIP：

```text
工具目录/
  results/
    AndroidLogs_yyyyMMdd_HHmmss_设备序列号/
    AndroidLogs_yyyyMMdd_HHmmss_设备序列号.zip
```

ZIP 中主要包含：

- `logcat-all.txt`：从采集开始持续抓取的完整 Logcat，包含普通运行日志及 `main`、`system`、`crash`、`events` 等缓冲区。
- `logcat-snapshot.txt`：按下回车时再次导出的 Logcat 快照。
- `screen-at-problem.png`：故障现场截图。
- `screen-recording.mp4`：选择启用录屏时保存的复现过程画面。
- `activity-lastranr.txt`：系统记录的最近一次 ANR。
- `dropbox-*-anr.txt`：Android Dropbox 中保存的 ANR 信息。
- `native-backtrace-*.txt`：目标进程 native 线程快照。
- Java `SIGQUIT` 线程信息：写入持续 Logcat 和 bugreport。
- `app-exit-info.txt`：Android 11 及以上的应用历史退出原因。
- CPU、内存、进程线程、Activity、Window、Input 和网络状态快照。
- `anr-files-from-device`：系统允许 ADB 读取时，从 `/data/anr` 拉取的 trace 文件。
- `bugreport.zip`：完整 Android 系统诊断包。
- `采集摘要.txt`：使用中文记录设备、包名、采集时间、采集前后 PID，以及录屏是否启用、录屏结果和文件名。

工具界面和采集摘要均使用中文。Logcat、异常堆栈和 bugreport 属于 Android 原始诊断数据，会保持系统输出的原文，以免破坏异常名称、类名和堆栈信息。

## 常见问题

### 找不到设备

检查以下项目：

- USB 调试是否打开；
- 手机是否已经允许当前电脑调试；
- USB 连接模式是否为“传输文件”；
- 数据线是否只支持充电；
- 是否需要安装手机厂商的 Windows USB 驱动。

设备显示 `unauthorized` 时，解锁手机并允许调试，然后在工具里重试。

### 无法自动下载 ADB

如果电脑已经安装 ADB，但没有配置环境变量，可以用记事本打开工具目录中的 `adb.config`，填写 `adb.exe` 完整路径：

```ini
ADB_PATH=C:\Users\tester\AppData\Local\Android\Sdk\platform-tools\adb.exe
```

也可以只填写 `platform-tools` 文件夹路径：

```ini
ADB_PATH=D:\Android\platform-tools
```

`ADB_PATH` 留空时默认从 Windows `PATH` 环境变量读取。配置文件中的值会覆盖环境变量；配置错误时工具会直接显示无效路径，不会静默使用其他 ADB。

如果电脑没有 ADB，并且无法访问 Google，可在另一台电脑下载 Google 官方 Android `platform-tools`，解压后将完整的 `platform-tools` 文件夹放在本工具目录中，并在 `adb.config` 中配置：

```text
Android日志采集工具/
  platform-tools/
    adb.exe
    AdbWinApi.dll
    AdbWinUsbApi.dll
    ...
```

```ini
ADB_PATH=platform-tools
```

随后重新双击启动脚本即可，全程不需要配置环境变量。

### `/data/anr` 显示 Permission denied

这是量产 Android 设备的正常权限限制，不代表采集失败。工具还会采集 `lastanr`、Dropbox ANR 和完整 bugreport，开发可从这些文件继续排查。不要为此对手机 root。

### 没弹 ANR 对话框也需要采集吗

需要。白屏、触摸无响应或长时间停在加载页不一定触发系统 ANR。持续 Logcat、线程快照、Window/Input 状态和 bugreport 对定位这类问题同样重要。

### 为什么录屏只有 180 秒或没有声音

这是 Android 原生 `screenrecord` 的常见限制，不是工具卡住或导出失败。需要录屏时请在工具显示“设备录屏已启动”后尽快开始复现。达到录屏时长上限后，持续 Logcat 和其他日志采集不会停止。

### 录屏失败会影响日志吗

不会。录屏是可选功能，设备不支持录屏、录屏命令提前结束或视频拉取失败时，工具会显示中文提示并继续采集 Logcat、现场状态和 bugreport。可以在 `采集摘要.txt` 和 `screen-recording-*.txt` 中查看录屏结果。

## 隐私说明

bugreport 可能包含设备型号、安装包信息、系统状态、部分系统日志和网络状态。
