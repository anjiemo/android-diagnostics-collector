# ADB 路径配置
#
# 默认将 ADB_PATH 留空，工具会从 Windows PATH 环境变量读取 adb.exe。
#
# 如需覆盖环境变量，可填写 adb.exe 完整路径：
# ADB_PATH=C:\Users\tester\AppData\Local\Android\Sdk\platform-tools\adb.exe
#
# 也可以填写 platform-tools 文件夹路径：
# ADB_PATH=D:\Android\platform-tools
#
# 相对路径以本工具所在目录为基准。

ADB_PATH=
