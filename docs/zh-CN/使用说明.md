# Android 日志采集工具使用说明

请选择对应平台的详细说明：

- [Windows 使用说明](windows.md)
- [macOS 使用说明](macos.md)

公共限制：Android 原生 `screenrecord` 通常最长 180 秒且不含设备音频。录屏失败不会中断 Logcat、现场快照或
bugreport 采集，结果会写入 `采集摘要.txt`。

公共输出位置：

```text
results/windows/AndroidLogs_yyyyMMdd_HHmmss_SERIAL/
results/macos/AndroidLogs_yyyyMMdd_HHmmss_SERIAL/
```

## 录屏限制

Android 原生 `screenrecord` 通常最长 180 秒且不含设备音频。录屏失败不会中断 Logcat、现场快照或 bugreport 采集，
结果会写入 `采集摘要.txt`。

## 输出位置

```text
results/windows/AndroidLogs_yyyyMMdd_HHmmss_SERIAL/
results/macos/AndroidLogs_yyyyMMdd_HHmmss_SERIAL/
```
